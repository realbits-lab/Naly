# ===========================================
# AI Provider Configuration
# ===========================================

# Image Generation Provider
# Options: "gemini" (default) | "ai-server"
IMAGE_GENERATION_PROVIDER=gemini

# Text Generation Provider
# Options: "gemini" (default) | "ai-server"
TEXT_GENERATION_PROVIDER=gemini

# ===========================================
# Gemini API Configuration (default provider)
# ===========================================
# Get your API key from: https://ai.google.dev/
GOOGLE_GENERATIVE_AI_API_KEY=your_google_api_key_here

# ===========================================
# AI Server Configuration (optional)
# ===========================================
# Use these when IMAGE_GENERATION_PROVIDER or TEXT_GENERATION_PROVIDER is "ai-server"
# API key is read from .auth/user.json (develop.profiles.writer.apiKey)

# AI Server URL for image generation
AI_SERVER_IMAGE_URL=http://localhost:8000

# AI Server URL for text generation
AI_SERVER_TEXT_URL=http://localhost:8000

# ===========================================
# Database Configuration
# ===========================================
DATABASE_URL=your_database_url_here

# ===========================================
# Vercel Blob Storage
# ===========================================
BLOB_READ_WRITE_TOKEN=your_blob_token_here

# ===========================================
# Vercel Cron Jobs
# ===========================================
# Secret token for securing cron job endpoints
# Generate a random string with at least 16 characters
# Use: openssl rand -base64 32 or 1Password's password generator
CRON_SECRET=generate-a-random-secret-min-16-chars

# ===========================================
# Redis Configuration
# ===========================================
# Redis connection URL for caching
# Format: redis://username:password@hostname:port/database
# Example for local: redis://localhost:6379
# Example for Upstash: redis://:password@region.upstash.io:6379
# Leave empty to disable caching (graceful degradation)
REDIS_URL=

# ===========================================
# NextAuth Configuration
# ===========================================
# Base URL for the application (used for OAuth callbacks and internal API calls)
NEXTAUTH_URL=http://localhost:3000
